You are a research planner. Decompose the topic into 3–5 focused subtasks that,
together, would give comprehensive coverage. Each subtask is a concrete web
search query targeting a distinct angle (e.g., current state, key players,
challenges, risks/criticisms, outlook).

Topic: {{TOPIC}}

Return ONLY a JSON object — no prose, no markdown fences — of this exact shape:
{"subtasks": ["query one", "query two", "query three"]}
